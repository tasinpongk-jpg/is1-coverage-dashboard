# Pinned for daily-build.yml — keep deps minimal.
# setsmart_proxy.py only needs FastAPI/httpx; build_daily.py runs the routes in-process.
fastapi==0.115.4
httpx==0.27.2
# DuckDB only used if SURVEILLANCE_DB_PATH is set; harmless to install.
# In CI the env var is unset → live SET news fallback is used.
duckdb==1.5.2
# pypdf — Loop 4 v5 raw markdown extraction (PDF text from MD&A / Auditor reports).
# No stdlib alternative for PDF text. ~3MB. Used by scripts/enrich_filing.py.
# Pinned to 6.x — 5.x fails with AttributeError on PIL.__version__ when
# Pillow is installed alongside.
pypdf==6.15.0

# pypdfium2 — pure-Python PDF page renderer for OCR fallback (scanned PDFs).
# Used by scripts/harvest_download.py when pypdf returns 0 text from a
# scanned/image-only PDF. Without pypdfium2, those filings fall back to
# 'needs_review:scanned_pdf' status until manually OCR'd.
pypdfium2==5.5.1
