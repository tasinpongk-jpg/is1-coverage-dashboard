# Pinned for daily-build.yml — keep deps minimal.
# setsmart_proxy.py only needs FastAPI/httpx; build_daily.py runs the routes in-process.
fastapi==0.115.4
httpx==0.27.2
# DuckDB only used if SURVEILLANCE_DB_PATH is set; harmless to install.
# In CI the env var is unset → live SET news fallback is used.
duckdb==1.5.2
