# Exclude non-static files from the Cloudflare Workers asset upload.
# Only HTML pages at root + data/*.json need to ship to the edge.
.git/
.github/
scripts/
surveillance/
logs/
*.md
*.ps1
requirements.txt
.gitignore
